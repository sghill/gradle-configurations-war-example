plugins {
    id 'java'
    id 'war'
    id 'nebula.provided-base' version '3.1.0'
}

allprojects {
    apply plugin: 'java'
    apply plugin: 'nebula.provided-base'

    repositories {
        jcenter()
    }
}
